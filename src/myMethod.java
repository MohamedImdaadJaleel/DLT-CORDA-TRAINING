import java.util.ArrayList;
import java.util.List;

public class myMethod {
    static void myMethod(String HEADR, String TRADE) {
        List<String> CSV = new ArrayList();
        CSV.add(HEADR);
//        CSV.add(TRADE);

        System.out.println(CSV);
    }
}
